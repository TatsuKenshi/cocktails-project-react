import React from "react";
import gif from "../assets/transparent-loading.gif";

const Loading = () => {
  return (
    <>
      <img src={gif} alt="loading spinner" />
    </>
  );
};

export default Loading;
